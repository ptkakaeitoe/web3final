{
  "name": "mystery-curve",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "dev": "vite",
    "build": "vite build",
    "metadata:prepare": "node scripts/prepare-metadata.js",
    "deploy:local": "hardhat run scripts/deploy.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:nft-suite:sepolia": "hardhat run scripts/deploy-nft-suite.js --network sepolia",
    "deploy:features:sepolia": "hardhat run scripts/deploy-features.js --network sepolia",
    "verify:sepolia": "hardhat run scripts/verify.js --network sepolia"
  },
  "dependencies": {
    "ethers": "^6.13.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17",
    "vite": "^6.0.7"
  }
}
